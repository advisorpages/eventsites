---
layout: layouts/base.njk
title: "Upcoming Events"
---

{% include "nav.njk" %}

<section class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
  <header class="text-center mb-10">
    <h1 class="text-4xl sm:text-5xl font-extrabold text-gray-900 mb-2">🎉 Upcoming Events</h1>
    <p class="text-gray-600 text-base sm:text-lg">
      Browse our upcoming trainings, team nights, and incentive events.
    </p>
  </header>

  <div class="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-5">
    {% for event in collections.events %}
      <a href="/eventsites{{ event.url | replace('/index.html', '/') }}"
         class="block bg-white p-5 rounded-lg shadow-sm border hover:shadow-md transition">
        
        <h2 class="text-lg font-semibold text-blue-800 mb-1">
          {{ event.data.title }}
        </h2>

        {% if event.data.date %}
          <p class="text-sm text-gray-600">📅 {{ event.data.date | date("cccc, LLLL d") }}</p>
        {% endif %}

        {% if event.data.event_location %}
          <p class="text-sm text-gray-600">📍 {{ event.data.event_location }}</p>
        {% endif %}

        {% if event.data.description %}
          <p class="text-sm text-gray-500 mt-2 line-clamp-3">{{ event.data.description }}</p>
        {% endif %}
        
      </a>
    {% endfor %}
  </div>

  <footer class="text-center mt-12">
    <a href="/eventsites/whos-registered/" class="inline-block text-blue-600 hover:text-blue-800 font-medium text-sm border-b border-blue-500">
      👥 Who’s Registered?
    </a>
  </footer>
</section>
