---
layout: layouts/base.njk
title: "Upcoming Events"
---

<section class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
  <header class="text-center mb-12">
    <h1 class="text-4xl sm:text-5xl font-extrabold text-gray-900 mb-3">Upcoming Events</h1>
    <p class="text-gray-600 text-base sm:text-lg">
      Join us at one of our exciting upcoming trainings, workshops, or team events.
    </p>
  </header>

  <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
    {% for event in collections.events %}
      <a href="/eventsites{{ event.url | replace('/index.html', '/') }}"
         class="block bg-white p-6 rounded-xl shadow-md hover:shadow-lg transition border border-gray-100 hover:border-blue-300">
        
        <h2 class="text-xl font-semibold text-blue-800 mb-1">
          {{ event.data.title }}
        </h2>

        {% if event.data.date %}
          <p class="text-sm text-gray-600">
            📅 {{ event.data.date | date("cccc, LLLL d") }}
          </p>
        {% endif %}

        {% if event.data.event_location %}
          <p class="text-sm text-gray-600">
            📍 {{ event.data.event_location }}
          </p>
        {% endif %}

        {% if event.data.description %}
          <p class="text-sm text-gray-500 mt-2 line-clamp-3">
            {{ event.data.description }}
          </p>
        {% endif %}
        
      </a>
    {% endfor %}
  </div>

  <footer class="text-center mt-16">
    <a href="/eventsites/whos-registered/" class="inline-block text-blue-600 hover:text-blue-800 font-medium text-sm border-b border-blue-500">
      👥 Who’s Registered?
    </a>
  </footer>
</section>
