<section class="text-center py-8">
  <img src="{{ speaker.image }}" alt="{{ speaker.name }}" class="mx-auto rounded-full w-32 h-32 mb-4" />
  <h2 class="text-xl font-bold">{{ speaker.name }}</h2>
  <p>{{ speaker.bio }}</p>
</section>
